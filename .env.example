# Environment Variables for Vercel Deployment

# Optional database (if you want to use a real database instead of memory)
# DATABASE_URL=your_neon_database_url_here

# Node Environment
NODE_ENV=production

# Port (Vercel will handle this automatically)
PORT=3000
